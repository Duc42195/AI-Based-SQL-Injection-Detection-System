"""FastAPI service exposing the detection pipeline and Admin review endpoints.

The application object lives in :mod:`deploy.main` (``app``). Run with:
``uv run uvicorn deploy.main:app --reload``.
"""
